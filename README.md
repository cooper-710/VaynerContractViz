
  # Boras Contract Webapp

  This is a code bundle for Boras Contract Webapp. The original project is available at https://www.figma.com/design/kLaLIS9kUbU4t1hGZdW8xC/Boras-Contract-Webapp.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  